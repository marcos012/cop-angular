import { ArrayFormatPipe } from './array-format.pipe';

describe('ArrayFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
